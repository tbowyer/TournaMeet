<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  
  <h1>Match details for <%= @match.player1_id %></h1>
  <center>
   <%= "#{@match.player1_id}" %>
   <br />
   <%= "#{User.find(TournamentUser.find(@match.player1_id.to_i).user_id).email}" + " vs." "#{User.find(TournamentUser.find(@match.player2_id.to_i).user_id).email}" %> 
  </center>
</div>
